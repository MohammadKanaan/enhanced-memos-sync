import { Plugin } from "obsidian";

export default class EnhancedMemosSyncPlugin extends Plugin {
  async onload(): Promise<void> {
    // Features are registered as their host-independent modules are completed.
  }
}
